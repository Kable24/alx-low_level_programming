#include "main.h"
/**
 *print_alphabet - prints the alphabet
 * Return: 0 always (success)
 */
void print_alphabet_x10(void)
{
	char ch = 'a';
int i = 0;
	for (ch = 'a'; ch <= 'z'; ch++)(;i<=10;i++)
		_putchar(ch);

	_putchar('\n');

}
